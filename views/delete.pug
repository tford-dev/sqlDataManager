extends layout

block content
  h2= title
  p 
    | Are you sure you want to delete 
    em= book.title
    | ?
  form(action="/books/" + book.id + "/delete", method="post")
    a(href="/books/" + book.id) Go back 
    | 
    input(type="submit", value = "I am sure, delete book.")